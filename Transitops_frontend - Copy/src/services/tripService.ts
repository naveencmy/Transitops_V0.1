export const tripService = {async getTrips(): Promise<Trip[]> {return mockRequest(trips);},async getTripById(id: string): Promise<Trip | undefined> {return mockRequest(trips.find((t) => t.id === id));},async getTripsByDriver(driverId: string): Promise<Trip[]> {return mockRequest(trips.filter((t) => t.driverId === driverId));},async getTripsByVehicle(vehicleId: string): Promise<Trip[]> {return mockRequest(trips.filter((t) => t.vehicleId === vehicleId));},};
